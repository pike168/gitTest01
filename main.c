//version 0.3a of main.c
#include <stdio.h>
#include "header.h"
int main()
{
    foo a  = malloc(sizeof(new foo));
    printf("edited the source\n");
    printf("Done here!\n");
    void **a = malloc(1024);
	return 0;
}
